(function (enyo, scope) {

	/**
	* Features a variety of commonly used widgets, including toolbars, text inputs, checkboxes, groups
	* and multiple types of buttons. 
	*
	* @namespace onyx 
	*/

	if (enyo && enyo.version) {
		enyo.version.moment = '2.9.0';
	}

})(enyo, this);